#!/bin/bash

# Usage: getrawstr "Input string"
# Output: "Input\ string"
function main() {
	rawFile="$(printf "%q" "${1}")"
	rawFile=${rawFile%\\\"}		# remove suffix
	rawFile=${rawFile#\\\"}		# remove prefix
	echo "${rawFile}"
}

main "$*"